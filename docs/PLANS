1. Read requirements - done
2. Choose tech stack(node,react,sqlite,docker,nx) - done
3. Env setup
  * setup monorepo - done
  * setup docker - done
  * api file structure - done
  * add db service(sqlite) - done
  * created DB schema - done
4. Backend development
  * gather books data(done)
  * create migration task to populate book data (done)
  * Make API Operations
    * users(authentication) - login, register(done)
    * books - list(done), CRUD(done)
    * favorites - add, remove, list(done)
  * Add ORM in API - done
  * API testing - done
5. Frontend development
  * Make a draft of the UI, layout - done
  * Break down components - header, booklist, bookcard, book item, bookmodal, login form, registration form, filters, search field
  * Create a folder structure (done)
  * Create reusable components(done)
  * Make test(done)
  * Integrate FE and API(done)
  * Features
    * books (done)
      - view book details(done)
      - search by books by title and author,(done)
    * login/register
    * favorites

Checklist
1. Responsive UI(done)
2. Book CRUD(API only), (done)
   User can browser books(FE),(done)
   search by title and author(FE),(done)
   book details(FE)(done)
3. DB Schema(done)
4. Error Handling in FE and API(done)
5. Documentation(WIP)

Bonus
* User login
  * Update header
* Favorites
* Tests